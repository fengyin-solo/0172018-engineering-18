/* ========================================
   构建信息（本地开发默认值）

   说明：镜像构建时本文件会被 Dockerfile 根据
   REPORT_VERSION / DATA_UPDATED_AT 构建参数覆盖；
   本地直接打开页面时使用此默认值，保证两种
   打开方式下页面结构与行为一致。
   ======================================== */

window.__BUILD_INFO__ = {
    reportVersion: '1.0.0',
    dataUpdatedAt: '2026-02-05'
};
